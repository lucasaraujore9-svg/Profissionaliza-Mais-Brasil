# Task: Commands Only

Gerar os 6 slash commands para um projeto existente.

## Pre-requisitos
- `CLAUDE.md` ou dados suficientes sobre stack e estrutura.

## Execucao

1. **Command Smith** analisa o projeto (le CLAUDE.md se existir)
2. Gera 6 comandos em `.claude/commands/`
3. Revisao via checklist (apenas itens de commands)

## Output
6 arquivos `.md` em `.claude/commands/`.
