// Stub vazio para o pacote `server-only` durante os testes (vitest roda em
// ambiente node, fora do runtime do Next que provê esse marker). Permite
// unit-testar funções puras exportadas de módulos server-only.
export {}
