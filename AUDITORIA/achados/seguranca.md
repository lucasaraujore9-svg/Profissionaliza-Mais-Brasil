# Auditoria — Segurança
_Data: 2026-07-03 · Referência: .claude/skills/auditoria-saas/references/01-seguranca.md · Itens do inventário cobertos: 309 route handlers (406 métodos) + 3 Server Actions + camada auth/lib + proxy + next.config + 3 webhooks + 17 crons + migrations · RE-VERIFICAÇÃO da auditoria de 2026-06-24 (delta `fa70037..a42ccb8`, 43 commits, 278 arquivos)_

## Resumo
- Itens verificados: 309 route handlers, incl. **15 handlers NOVOS do delta** (`admin/equipe/[id]/impersonate`, `admin/relatorios/bi/[tab]`, `painel/relatorios/bi/[tab]`, `painel/relatorios/export[/[report]]`, `aluno/comprar/{installments,process,status}`, `checkout/{enrollment/[id],installments}`, `loja/checkout/installments`, `painel/config/parcelamento`, `painel/placar/stream`, `tours/dismiss`, `cron/fix-gateway-collapse`), a camada `src/lib/checkout/*` (assert-tenant-gateway + issue-pmb-asaas-charge), `src/lib/reports/**` (BI admin+painel + aggregations), `src/lib/auth/scope.ts`, `src/lib/auth/roles.ts`, `next.config.ts`, `.env.example`, `src/lib/webhooks/lms-webhook.ts`, `src/lib/vercel/domain-status.ts`, `npm audit --omit=dev`, `npx tsc --noEmit` (exit 0).
- **Achados: P0=0 · P1=2 · P2=2 · P3=3 · Nota do domínio: 8.0/10**
- Postura geral **madura**, mas a nota caiu de 8.5 para 8.0 por um achado NOVO de authz-scoping (SEG-009). A blindagem do gateway revenda→PMB (P0 8541afd) foi auditada em profundidade e está **sólida**: `assertPmbCharge` guarda todo caminho que cobra na conta-mãe (`issue-pmb-asaas-charge.ts:118`, falha-fechada em matrícula inexistente), a recompra do aluno (`aluno/comprar/process`) usa exclusivamente as credenciais do próprio tenant e autoriza por `session.studentId`, e a cron de remediação (`fix-gateway-collapse`) é CRON_SECRET + re-check ao vivo + fail-safe. O webhook receiver do LMS mantém HMAC-SHA256 timing-safe + anti-replay + raw-body. **SEG-008 foi CORRIGIDO** (`.env.example:68`). O achado novo (SEG-009) é o hub de BI (`/admin/relatorios`) expondo métricas de todo o ecossistema a papéis internos de escopo limitado.

## Achados

### [SEG-009] Hub de BI do admin expõe dados de TODO o ecossistema a papéis de escopo limitado (authz-scoping ausente)
- **Severidade:** P1
- **Status:** Corrigido (2026-07-03) — gating por papel espelhando o export CSV. Abas de agregação global restringidas em `src/lib/reports/tabs.ts` (single source consumido pelo dispatcher `bi/[tab]/route.ts` via `canViewTab` e pela page via `allowedTabs`/`defaultTab`): `visao-geral` → `[SUPER_ADMIN, PMB_FINANCEIRO]` (removidos PMB_SALES + os 3 papéis tenant-scoped que viam MRR/top-revendas/funil global); `alunos-matriculas` e `cursos-cupons` → `[SUPER_ADMIN]` (ecossistêmicos, sem variante PMB-escopada limpa; PMB_SALES obtém a visão PMB via export CSV `alunos-vitrine-pmb`). `receita-vendas` mantém PMB_SALES mas o módulo (`src/lib/reports/bi/receita-vendas.ts`) agora força `segment="pmb"` (novo helper puro `resolveSegment`, ignora o `segment` da query) e omite o donut "PMB × Revendedores" para não revelar o total das revendas — espelha o `vendas-vitrine-pmb` do CSV. Papéis tenant-scoped (PMB_SALES_MGR/PMB_REVENDA_SALES/PMB_RESELLER_MGR) seguem nas abas já escopadas (`rede-revendedores`, `leads-conversao`, `indicacoes-comissoes`). **Verificação:** `src/lib/reports/tabs.test.ts` (bloco "SEG-009 authz de agregação global": nenhuma das 4 abas globais é vista pelos papéis tenant-scoped; visao-geral só SUPER_ADMIN/PMB_FINANCEIRO; PMB_SALES sem alunos/cursos/visao-geral) + `src/lib/reports/bi/receita-vendas.test.ts` (PMB_SALES forçado a `pmb` mesmo pedindo `todos`/`revenda`; SUPER_ADMIN/PMB_FINANCEIRO respeitam a query). Portão Zero-Erro verde (typecheck/lint/build/337 testes). Corrigido no commit `fix(seguranca): escopa abas de BI global do admin por papel (SEG-009)`.
- **Status anterior:** Aberto
- **Local:** `src/lib/reports/bi/visao-geral.ts:16-160` (sem scope), `src/lib/reports/bi/receita-vendas.ts:25-55` (segment vem de query, não do papel), `src/lib/reports/bi/alunos-matriculas.ts:15-45` (sem scope), `src/lib/reports/bi/cursos-cupons.ts` (sem scope); dispatcher `src/app/api/admin/relatorios/bi/[tab]/route.ts:26-47` (só checa `canViewTab`, não escopa); allowlist `src/lib/reports/tabs.ts:27-86`; papéis aceitos `src/lib/auth/admin-session.ts:11-18`.
- **Evidência:** `requireAdminSession` admite os 6 papéis PMB (`PMB_TEAM` inclui `PMB_SALES`, `PMB_SALES_MGR`, `PMB_REVENDA_SALES`, `PMB_RESELLER_MGR`, `PMB_FINANCEIRO`). O dispatcher `/api/admin/relatorios/bi/[tab]` gate apenas por `canViewTab(session.role, tab)` e delega ao módulo com `{ session, period, sp }` — **sem** passar/aplicar `tenantScopeWhere`. Em `tabs.ts`, `visao-geral` tem `roles: ALL` (todos os 6 papéis). Só 3 dos 8 módulos escopam por papel (`rede-revendedores.ts:17`, `indicacoes-comissoes.ts`, `leads-conversao.ts` via `tenantScopeWhere`/`leadScopeWhere`). Os demais NÃO:
  - `visao-geral.ts:34-64` computa receita total (todos tenants + PMB), `MRR (revendas)` de TODAS as revendas ativas, contagem global de alunos/matrículas/tenants, funil global e **"Top revendas por receita"** (top-10 com NOME + GMV, `visao-geral.ts:135-158`). Os KPIs/tabela financeiros são liberados por `canViewFinance` = `[SUPER_ADMIN, PMB_FINANCEIRO, PMB_SALES, PMB_RESELLER_MGR]` (`roles.ts:13-18`). Logo um **PMB_RESELLER_MGR** — cujo escopo é só `accountManagerId` das unidades que atende (`scope.ts:45-46`) — vê receita/MRR do ecossistema inteiro + nomes+receita das 10 revendas que mais faturam (incl. as que ele NÃO gerencia). Os papéis não-finance (`PMB_SALES_MGR`, `PMB_REVENDA_SALES`) que alcançam a aba veem os contadores globais (alunos, revendas ativas, conversão, funil).
  - `receita-vendas.ts:20-34` (roles `SUPER_ADMIN, PMB_FINANCEIRO, PMB_SALES`): `segment` default `"todos"` → **PMB_SALES** (vendedor de curso B2C, que "não enxerga unidades/leads B2B", `scope.ts:15`) recebe receita de TODAS as revendas + breakdown PMB×revenda + pagamentos recentes.
  - `alunos-matriculas.ts:32-45` (roles `SUPER_ADMIN, PMB_SALES`): `prisma.student.count()`/`enrollment` sem filtro → PMB_SALES vê todos os alunos/matrículas do ecossistema (incl. de revendas).
  - **Inconsistência-prova:** o export CSV legado `/api/admin/relatorios/[type]/route.ts:27-86` JÁ enforce esse least-privilege (allowlist `[SUPER_ADMIN, PMB_SALES, PMB_RESELLER_MGR]`, `PMB_SALES` só `pmbSalesAllowed`, `PMB_RESELLER_MGR` obrigado a informar `tenantId` que ele gerencia). O hub de BI novo (delta 9cefc8d) reintroduziu o acesso amplo que o CSV bloqueia.
- **Impacto:** Quebra de menor-privilégio interno: papéis comerciais/de suporte de escopo limitado (gerente de conta, vendedor de revenda, vendedor de curso) acessam, via GET direto ao endpoint, receita agregada de todo o ecossistema, MRR de todas as revendas e a identidade+faturamento das revendas de maior GMV — dados competitivamente sensíveis fora do escopo autorizado desses papéis. Não é vazamento cross-tenant de cliente final (P0), mas é falha de AuthZ em endpoints específicos (P1).
- **Correção:** Escopar cada módulo de BI do admin pelo papel, espelhando o CSV export:
  1. Passar o escopo ao contexto: em `bi/context.ts` derivar `tenantWhere = await tenantScopeWhere(session)` e um `financeSegment` a partir do papel; injetar em `BiContext`.
  2. Em `visao-geral.ts`, `receita-vendas.ts`, `alunos-matriculas.ts`, `cursos-cupons.ts`: aplicar `...tenantWhere` (para Tenant) e derivar o `segment`/`tenantId` das queries de Payment/Student/Enrollment do papel — nunca do query param quando o papel é limitado. Para `PMB_SALES` forçar `segment="pmb"` (tenantId=null); para `PMB_RESELLER_MGR`/`PMB_REVENDA_SALES`/`PMB_SALES_MGR`, restringir Payment/Student/Enrollment às unidades do escopo (`tenantId in {ids do escopo}`); só `SUPER_ADMIN`/`PMB_FINANCEIRO` veem "todos".
  3. Alternativa mínima (defesa imediata sem redesenho): no dispatcher `admin/relatorios/bi/[tab]/route.ts`, se `session.role !== "SUPER_ADMIN" && !canViewFullFinance(session.role)`, negar as abas `visao-geral`/`receita-vendas`/`alunos-matriculas`/`cursos-cupons` (403) até o scoping por papel existir — ajustando `tabs.ts` para não listá-las a esses papéis.
- **Verificação:** teste de authz: logar como `PMB_RESELLER_MGR` (com 1 unidade atribuída), `GET /api/admin/relatorios/bi/visao-geral` → a tabela "Top revendas" e o MRR devem refletir SOMENTE as unidades do gerente (ou 403); repetir `receita-vendas` como `PMB_SALES` → só segment PMB. Adicionar caso no CI cobrindo cada aba × papel do `tabs.ts`.

### [SEG-001] Isolamento multi-tenant 100% em código — sem RLS como rede de segurança (defense-in-depth ausente)
- **Severidade:** P1
- **Status:** Aberto — adiado por decisão do dono (2026-07-03) para a migração VPS. RLS (item (b) da correção) é decisão arquitetural que muda o modelo de acesso e exige Supavisor/pgBouncer compatível com `SET LOCAL`; fica para a trilha de migração VPS. Item (a) (teste de isolamento no CI) não foi aplicado nesta rodada por estar fora do foco autorizado.
- **Local:** `prisma/migrations/**` (44 models, nenhum com RLS de tenant); `grep -rli "row level security" prisma/migrations` → só `20260430_notification_preferences` (`deny_anon`, fail-closed, tabela única) · memória do projeto: "sem RLS no banco — isolamento em código".
- **Evidência:** RE-VERIFICAÇÃO 2026-07-03: nenhuma tabela com `tenantId` (`Student`, `Enrollment`, `Payment`, `Coupon`, `Course`/`TenantCourse`, `Lead`, `Certificate`, `StudentNote`, `ContactMessage`, `CoursePackage`/`TenantPackage`, `ReferralCommission`/`ReferralMonthlyCommission`, `HomeSection`, etc.) tem `ENABLE ROW LEVEL SECURITY` + policy escopada. Único controle contra vazamento cross-tenant é o `where: { tenantId }` manual em cada query. O delta reforça o padrão correto (todos os handlers novos derivam o tenant da sessão: `painel/relatorios/bi` usa `ctx.tenantId` de `requireResellerSession`, `painel/placar/stream:22-33` usa `session.user.tenantId` + gate `canSellResellers`, `aluno/comprar/process:91` autoriza `enrollment.studentId === session.studentId`), mas a invariante segue dependendo de revisão humana a cada handler.
- **Impacto:** Um único `findMany`/`findFirst` futuro sem `where: { tenantId }` (ou `findUnique` por id sem checagem de posse) vaza dados entre revendas — cross-tenant é P0. Sem RLS não há contenção. Risco cresce na ⚠️MIGRAÇÃO p/ Postgres self-hosted (jobs, psql, ferramentas internas acessam o banco fora da app).
- **Correção:** (a) Mitigação barata e sem aprovação: teste de isolamento de tenant automatizado no CI — para cada model com `tenantId`, exercitar handler representativo com sessão do tenant A pedindo recurso de B e exigir 403/404/vazio. (b) Roadmap RLS: `ENABLE` + `FORCE ROW LEVEL SECURITY` nas tabelas com `tenantId`, policy via `set_config('app.tenant_id',...)` por request (`SET LOCAL` em transação Prisma). DECISÃO ARQUITETURAL — o corretor deve PARAR e descrever (b) ao usuário (muda o modelo de acesso e exige Supavisor/pgBouncer compatível com `SET LOCAL`).
- **Verificação:** `npm test -- tenant-isolation` verde; manual: logar como revenda A, `GET /api/painel/alunos/<id-da-revenda-B>` → 404; repetir cupons/certificates/financeiro/vendas/revendas/pacotes/relatórios. Se RLS: `SELECT relname, relrowsecurity, relforcerowsecurity FROM pg_class WHERE relname IN (<tabelas com tenantId>)` → `t` em ambas colunas.

### [SEG-002] CSP usa `script-src 'unsafe-inline' 'unsafe-eval'` — reduz proteção a XSS
- **Severidade:** P2
- **Status:** Aberto
- **Local:** `next.config.ts:34` (`"script-src 'self' 'unsafe-inline' 'unsafe-eval' https://sdk.mercadopago.com ..."`) e `:33` (`style-src 'self' 'unsafe-inline'`).
- **Evidência:** RE-VERIFICAÇÃO 2026-07-03: `script-src` inalterado (ainda `'unsafe-inline' 'unsafe-eval'`). O delta só ampliou `img-src`/`frame-src` para YouTube (Treinamentos, commit cbea442) e Mercado Pago — nada endureceu o `script-src`. Não há XSS reflexivo identificado (os `dangerouslySetInnerHTML` seguem só em snippets templated de `snippets.ts` com `sanitizeId()`+`JSON.stringify` e em JSON-LD de config estática; `git diff` do delta não adicionou novos `dangerouslySetInnerHTML`). Mas `'unsafe-inline'`/`'unsafe-eval'` removem a principal mitigação caso uma XSS surja.
- **Impacto:** Se uma XSS for introduzida (novo `dangerouslySetInnerHTML` com input de usuário, ou lib comprometida no bundle de pixels white-label que se propaga a todas as vitrines), a CSP não conterá o ataque — execução de script arbitrário no contexto da vitrine/painel.
- **Correção:** Migrar para CSP baseada em **nonce**: gerar nonce por request no `src/proxy.ts`, propagar via header e aplicar aos `<Script>` do next/script + snippets; trocar `script-src` para `'self' 'nonce-<n>' https://sdk.mercadopago.com ...` e remover `'unsafe-eval'` (validar se o Payment Brick exige `eval`; se sim, isolar em iframe sandbox). Manter `style-src 'unsafe-inline'` só se Tailwind/shadcn exigir.
- **Verificação:** `curl -sI https://profissionalizamaisbrasil.com.br | grep -i content-security-policy` sem `'unsafe-inline'`/`'unsafe-eval'` em `script-src`; smoke do checkout MP (Payment Brick), editor de certificado (iframe blob) e player YouTube dos Treinamentos continuam OK.

### [SEG-007] `nodemailer@7.0.13` HIGH (SMTP command injection) — sem patch upstream, baixa exploitabilidade
- **Severidade:** P2
- **Status:** Aceito (risco assumido) (2026-07-03) — não há release corrigido publicado (`nodemailer` mais recente é 7.0.13; o fix upstream exige ≥9.0.1, ainda inexistente) → bump impossível hoje, item (a) fica como watch. Item (b) VERIFICADO no código: `src/lib/email/smtp.ts:68-91` monta o transport só com `host`/`port`/`secure`/`auth.{user,pass}` de env `SMTP_*`, nunca seta `name`, e `sendSmtp` (`:112-118`) passa apenas `from`/`to`/`subject`/`html`/`replyTo` (sem `envelope`) — nenhum input de usuário alcança os vetores dos advisories (CRLF no `name`/EHLO; `envelope.size`). Item (c) FEITO: risco documentado em `docs/SECURITY.md §5 LOW` com watch item (bumpar em ≥9.0.1) e a invariante (SMTP por revenda não pode derivar `host`/`name`/`envelope` de dados de tenant sem sanitização CRLF). Sem mudança de comportamento — nenhum teste novo. Portão Zero-Erro verde. Commit `8b3aa6d`.
- **Status anterior:** Aberto
- **Local:** `src/lib/email/smtp.ts:68-91` (`nodemailer.createTransport({ host, port, secure, auth, ... })`); dep direta `nodemailer@7.0.13` (`package.json:46`) + transitiva via `next-auth@5.0.0-beta.31` e `@auth/prisma-adapter`.
- **Evidência:** `npm audit --omit=dev` reporta `nodemailer <=9.0.0 HIGH` com DOIS advisories: GHSA-vvjj-xcjg-gr5g (CRLF no `name`/EHLO) e GHSA-c7w3-x93f-qmm8 (`envelope.size`). `npm ls nodemailer` → 7.0.13 (mais recente publicada; sem patch upstream ≥9.0.1). RE-VERIFICAÇÃO: o transport é montado só com `host`/`port`/`secure`/`auth.user`/`auth.pass` de env (`SMTP_*`), timeouts e pool — a opção `name` NÃO é setada e nenhum `envelope` é passado (`sendSmtp` usa `from`/`to`/`subject`/`html`/`replyTo`, `smtp.ts:112-118`). Nenhum input de usuário chega aos vetores dos advisories.
- **Impacto:** Hoje baixíssimo (sem input de usuário no `name`/`envelope`; config SMTP é app-controlled). É uma HIGH aberta no supply-chain sem fix disponível; torna-se relevante se `name`/`host`/`envelope` do transport passarem a derivar de dados de tenant (ex.: SMTP por revenda).
- **Correção:** (a) Bumpar `nodemailer` assim que sair ≥9.0.1 (automatizável via SEG-005). (b) Garantir que `host`/`name`/`envelope` do transport nunca derivem de input de usuário/tenant. (c) Documentar o risco aceito em `docs/SECURITY.md` enquanto não houver patch.
- **Verificação:** após bump, `npm ls nodemailer` → ≥9.0.1 e `npm audit --omit=dev` sem o HIGH; envio de email de teste continua funcionando.

### [SEG-004] `mercadopago` (npm) é dependência direta não-usada e arrasta `uuid` vulnerável
- **Severidade:** P3
- **Status:** Corrigido (2026-07-03) — `npm rm mercadopago` (removido de `package.json` + lockfile). Confirmado zero import do SDK (`grep -rnE "from ['\"]mercadopago['\"]|require\(['\"]mercadopago['\"]" src scripts prisma` → NENHUM; a integração MP usa o cliente próprio `src/lib/mercadopago/client.ts` via `fetch`). `npm ls mercadopago` → `(empty)`; `npm audit --omit=dev` caiu de 14 → 13 vulns (subtree `uuid` via mercadopago eliminado). Documentado em `docs/SECURITY.md §5 LOW`. Portão Zero-Erro verde (npm ci + typecheck/lint/build/337 testes). Commit `__COMMIT_SEG004__`.
- **Status anterior:** Aberto
- **Local:** `package.json:42` (`"mercadopago": "^2.12.0"`).
- **Evidência:** RE-VERIFICAÇÃO 2026-07-03: `grep -rln "from \"mercadopago\"|require(.mercadopago" src` → vazio (integração MP usa cliente próprio `src/lib/mercadopago/client.ts` via `fetch`). `npm audit` aponta `mercadopago 1.0.0-3.0.0 → uuid <11.1.1` (GHSA-w5hq-g745-h8pq, moderate). `uuid` também entra por `exceljs` (escrita-apenas, risco aceito), mas `mercadopago` é peso morto puro.
- **Impacto:** Aumenta superfície de supply-chain sem benefício; código morto. Sem impacto direto em runtime.
- **Correção:** `npm rm mercadopago` e atualizar lockfile, confirmando que nada importa o SDK. Rodar o Portão Zero-Erro.
- **Verificação:** `npm ls mercadopago` → "not found"; `npm audit --omit=dev` sem o subtree `uuid` via mercadopago; `npx tsc --noEmit` + `next build` verdes.

### [SEG-005] Sem Dependabot/Renovate e CI de audit não-bloqueante — vulnerabilidades sem visibilidade ativa
- **Severidade:** P3
- **Status:** Aberto
- **Local:** ausência de `.github/dependabot.yml`/`renovate.json` (confirmado: `ls` → NONE); `.github/workflows/ci.yml:55-60` tem `npm audit --omit=dev --audit-level=high` com `continue-on-error: true` ("audit não bloqueia").
- **Evidência:** RE-VERIFICAÇÃO 2026-07-03: sem automação de bump. As 14 vulns atuais (12 moderate, 2 high) ficam visíveis só na PR, sem gate e sem PRs automáticos. Nota de packaging: `prisma@7.8.0` e `shadcn@4.2.0` estão em `dependencies` (não devDeps) e arrastam `hono@4.12.22` (HIGH GHSA-wwfh-h76j-fc44 / GHSA-j6c9-x7qj-28xf) para a árvore de produção; `hono` não é importado em runtime (é o dev-server do Prisma / CLI shadcn / MCP SDK), então não entra no bundle Next — mas move ambos para `devDependencies` limparia a HIGH do audit `--omit=dev`.
- **Impacto:** Vulnerabilidades de libs (auth/crypto/email/Next) podem permanecer abertas por longos períodos. ⚠️MIGRAÇÃO: na VPS, sem o radar da Vercel, fica ainda mais relevante.
- **Correção:** (a) `.github/dependabot.yml` (ecosystem `npm`, weekly, agrupando dev-deps) OU `renovate.json`. (b) Mover `prisma`/`shadcn` para `devDependencies` (some a `hono` HIGH do audit de prod). (c) Opcional: tornar o step de audit bloqueante para HIGH/CRITICAL NOVAS (allowlist das aceitas). Atualizar o comentário `ci.yml:55` (menciona "xlsx", já removido).
- **Verificação:** PR automático do Dependabot/Renovate aparece; `npm audit --omit=dev` deixa de listar `hono` HIGH; o job de audit falha o CI numa HIGH/CRITICAL não-allowlistada.

### [SEG-006] Verificação manual: confirmar segredos críticos em produção (incl. MP_WEBHOOK_SECRET / PMB_WEBHOOK_SECRET) + plano de rotação
- **Severidade:** P3
- **Status:** Aberto
- **Local:** `src/lib/env.ts` (envs `requiredInProd`/warnings) — não auditável a partir do repo (env de produção).
- **Evidência:** O código fail-fasta no boot quando faltam `AUTH_SECRET`, `ENCRYPTION_KEY`, `CRON_SECRET`, `INTERNAL_SECRET`, `ASAAS_WEBHOOK_TOKEN`, `EA_*`, `ASAAS_*`. `MP_WEBHOOK_SECRET`, `PMB_WEBHOOK_SECRET`, `LMS_API_*` e Upstash apenas emitem **warning** (degradam features). Sem `PMB_WEBHOOK_SECRET`, o receiver `/api/webhooks/lms/route.ts:50-60` responde 503 e os webhooks de entrada do LMS ficam desligados. A memória do projeto registra blocker ABERTO: `MP_WEBHOOK_SECRET` ausente no Vercel + `notification_url` no apex impedem o fulfillment da venda MP da vitrine PMB.
- **Impacto:** `MP_WEBHOOK_SECRET` ausente → vendas da vitrine PMB (tenantId=null) sem matrícula automática. `PMB_WEBHOOK_SECRET` ausente → certificado/progresso/suporte do LMS não chegam em tempo real (degrada). `UPSTASH_*` ausente → rate-limit desligado (buckets failOpen liberam; demais negam em prod). Sem runbook de rotação de `ENCRYPTION_KEY` (rotacioná-la quebra a leitura de `mpAccessToken`/`asaasApiKey`/`asaasWebhookToken`/`mpWebhookSecret`/`lmsSenha` já cifrados — exige re-cifragem com 2 chaves na transição).
- **Correção:** (manual) Confirmar no Vercel: `MP_WEBHOOK_SECRET`, `PMB_WEBHOOK_SECRET` (≥16 chars), `UPSTASH_REDIS_REST_URL`/`TOKEN`, `LMS_API_URL`/`LMS_API_KEY`, além dos `requiredInProd`. Documentar em `docs/` um runbook de **rotação de chaves** — em especial que rotacionar `ENCRYPTION_KEY` exige migration de re-cifragem de TODOS os campos cifrados (incl. `Enrollment.lmsSenha` e `Student.plataformaAlunoSenha`), suportando 2 chaves na transição.
- **Verificação:** `vercel env ls production` mostra as chaves; entrega de teste assinada para `/api/webhooks/lms` retorna 200 (não 503); uma venda de teste na vitrine PMB matricula automaticamente.

## Fechados nesta rodada

### [SEG-008] `.env.example` não documentava `PMB_WEBHOOK_SECRET` — CORRIGIDO
- **Severidade:** P3 · **Status:** ✅ Corrigido (commit d44c865)
- **Evidência:** `grep -n PMB_WEBHOOK_SECRET .env.example` → linha 68 (`PMB_WEBHOOK_SECRET=`, sem valor real). A referência §6 (env.example completo) satisfeita.

### [SEG-003] `xlsx` HIGH (prototype pollution + ReDoS) — CONFIRMADO CORRIGIDO
- **Severidade:** P2 · **Status:** ✅ Corrigido (2026-06-20; re-confirmado 2026-07-03)
- **Evidência:** `grep -nE "xlsx" package.json` → vazio; `exceljs ^4.4.0` (linha 37) no lugar; `npm audit --omit=dev` não lista mais `xlsx`. Export via ExcelJS.

## Cobertura

**Isolamento multi-tenant (ref §1):** 309 route handlers cobertos. Handlers do delta:
- **BI painel** (`painel/relatorios/bi/[tab]`, `.../export[/[report]]`) → `requireResellerSession` + `ctx.tenantId` é o âncora; TODOS os módulos `src/lib/reports/painel/*` filtram por `tenantId`/`referrerTenantId` (verificado query a query: financeiro/alunos/visao-geral/indicacoes/cursos-cupons/receita). Abas owner-only (`indicacoes`) reforçadas server-side (`tabs.ts:32-36` + `route.ts:43`). OK.
- **BI admin** (`admin/relatorios/bi/[tab]`) → `requireAdminSession` + `canViewTab`; **SEG-009 CORRIGIDO** (2026-07-03): abas de agregação global restritas a papéis com visão de ecossistema (`tabs.ts`) e `receita-vendas` força `segment="pmb"` para PMB_SALES.
- **placar/stream** → `auth()` + `session.user.tenantId` + gate `canSellResellers` (`route.ts:22-34`); snapshot escopado por `referrerTenantId`. OK.
- **aluno/comprar/process** → autoriza `enrollment.studentId === session.studentId` (`route.ts:91`), usa credenciais do PRÓPRIO tenant (`decryptTenantMpToken`/`decryptTenantAsaasKey`), nunca a conta-mãe. OK.
- **checkout/enrollment/[id]** → `isPmbAppHost` gate + `findFirst({ tenantId: null, gateway: ASAAS })` + `issuePmbAsaasCharge` (que reexecuta `assertPmbCharge`). Endpoint público de payment-link (por design, rate-limited); sem vazamento de dados na resposta. OK.
- **tours/dismiss** → IDs derivados da sessão (User/Student), Zod (`tourId` max 120), idempotente. OK.
- **admin/equipe/[id]/impersonate** → `SUPER_ADMIN`-only (`route.ts:32-38`), allowlist de papéis internos, bloqueia self/SUPER_ADMIN/inativo, `startImpersonation` HMAC + `logAudit`. OK.
- **Nenhum handler do delta lê `tenantId` de body/query** (grep negativo, exceto os 2 admin `searchParams tenantId` legados de `admin/certificates` e `admin/relatorios/[type]`, ambos com role-gate/ownership).
- `using (true)`/`with check (true)`: N/A — sem policies de tenant; única policy é `deny_anon`. RLS ausente: **SEG-001**.

**Chaves Supabase / superfície de cliente (ref §2):** OK — `grep service_role src/components` → vazio; nenhum `NEXT_PUBLIC_*` com nome de segredo; `SUPABASE_SERVICE_ROLE_KEY` só server (`certificates/storage.ts`, `supabase/storage.ts`); `LMS_API_KEY` só Bearer server-side (`src/lib/lms/client.ts`). Aba API expõe `PMB_WEBHOOK_SECRET` só quando `role==="SUPER_ADMIN"`.

**Autenticação (ref §3):** OK — NextAuth v5 (`auth()` server-side, equivalente a `getUser()`; **zero `getSession()`** no repo). Login rate-limited; JWT re-sincroniza papel/status a cada 60s. Cookies httpOnly+secure(prod)+sameSite=lax. Novo `painel/config/parcelamento` valida `role==="RESELLER"` + `tenantId`.

**Autorização (ref §4):** OK — guards centralizados corretos; hub BI admin **SEG-009 CORRIGIDO** (2026-07-03): as 4 abas de agregação global agora seguem o least-privilege do CSV (gating por papel em `tabs.ts` + `receita-vendas` força PMB para PMB_SALES). `scope.ts` (`tenantScopeWhere`/`canAccessTenantScope`/`leadScopeWhere`) modela o escopo por papel corretamente. Impersonação de equipe interna é SUPER_ADMIN-only + audit. Consultor cap de desconto validado server-side.

**Validação de entrada (ref §5):** OK — Zod nos boundaries de todos os handlers novos (`bodySchema` em tours/parcelamento/aluno-comprar/checkout-enrollment/pay-card). `aggregations.ts` usa `$queryRawUnsafe` com args parametrizados ($1..$N) e `bucket` validado por whitelist (`safeBucket`), `tenant_id = $N` bind — **sem SQLi**. **SSRF: N/A** — `fetch` de URL de usuário não encontrado; `domain-status.ts`/`dominio/verify` usam `tenant.customDomain` do banco, não input cru. **XSS:** sem novos `dangerouslySetInnerHTML` (ver SEG-002).

**Segredos (ref §6):** OK — `.env*` no `.gitignore`; `git log -p` de segredos reais → vazio; `.env.example` agora completo (SEG-008 fechado). Rotação: **SEG-006**.

**Headers/borda (ref §7):** Parcial — `next.config.ts:7-47` com HSTS, nosniff, X-Frame-Options=DENY, Referrer-Policy, Permissions-Policy, CSP com `frame-ancestors 'none'`/`object-src 'none'`/`base-uri 'self'`. CSP fraca em script-src: **SEG-002**. Rate-limit nos handlers públicos novos (`aluno/comprar/process`, `checkout/enrollment/[id]`, `cobranca/pay-card` via `RATE_LIMITS.publicCheckout`/`cobrancaPayCard`). Webhooks com assinatura: Asaas token timing-safe, MP HMAC (exige x-signature em prod), LMS HMAC-SHA256 timing-safe + anti-replay 10min + raw-body (`lms-webhook.ts:37-69`, `webhooks/lms/route.ts:70-73`), idempotente via `WebhookLog.externalEventId @unique` + `lmsDedupKey` (hash de conteúdo quando falta o header). Crons: 17/17 com `isCronAuthorized` (CRON_SECRET `safeEqual`); `fix-gateway-collapse` idem (GET+POST compartilham handler autorizado, `route.ts:57-59`).

**Dependências (ref §8):** `npm audit --omit=dev` → 14 vulns (12 moderate, 2 high). **SEG-003 (xlsx) FECHADO**. Abertos: SEG-007 (`nodemailer` HIGH, sem patch, baixa exploitabilidade), SEG-004 (`mercadopago` não-usado → `uuid`), SEG-005 (sem Dependabot + CI audit não-bloqueante + `hono` HIGH via `prisma`/`shadcn` em `dependencies`). `dompurify`/`postcss`/`uuid` moderate — monitorar/risco aceito.

**Itens N/A:** RLS policy-a-policy (coberto por SEG-001 e domínio `banco`); Server Actions (3: preview/validação read-only sem mutação sensível); CORS `*` com credentials (ausente no repo); `metrics/public`, `placar/stream` público e `installments` quotes (agregados/simulações sem PII, por design); `cobranca/[paymentId]/pay-card` e `checkout/enrollment/[id]` (endpoints públicos de payment-link por design — `isKnownAsaasPayment`/`isPmbAppHost` + rate-limit, sem leitura de dado sensível na resposta).
